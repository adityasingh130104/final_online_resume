import { AppSolution, Border, Button, Container, FadeIn, FadeInStagger, Section } from '@/components';
import Image from 'next/image';


export const appData = {
  industry: 'C Programming',
  title: 'C Compiler for Syntax Error Identification',
  description: 'This is a basic C compiler that identifies 15 common syntax errors in C code provided by the user.',
  date: '2024-12',
  service: 'C Programming Mini Project',
  url: 'https://github.com/nikhil8424/C-compiler-identifying-syntax-erroes',
  pathname: '/apps/leenith-borges',
  framework: 'C',
};

export const sections = [
  { index: 0, title: 'About', id: 'about' },
  { index: 1, title: 'Challenge', id: 'challenge' },
  { index: 2, title: 'Solution', id: 'solution' },
  { index: 3, title: 'Technologies', id: 'technologies' },
];

export const metadata = {
  title: `${appData.title}`,
  description: appData.description,
};

export const technologies = [
  {
    name: 'C Programming',
    image: '/logos/cporgramming.png',
  },
  {
    name: 'Git',
    image: '/logos/git-logo.png',
  },
  {
    name: 'Github',
    image: '/logos/github-logo.webp',
  },
];

<Container>
  <Border className="my-8" />

  <Section id="challenge">
    ## Challenge

    I created a basic C compiler that can identify syntax errors in the code provided by the user. The task was to build a simple tool that can:
    
    - Accept a user-provided code line by line.
    - Identify common syntax errors in C programming, such as missing semicolons, parentheses, and other basic errors.
    - Display a detailed list of errors after analyzing the code.

    The challenges included handling user input effectively and correctly identifying common syntax errors.

  </Section>

  <Border className="my-8" />

  <Section id="solution">
    ## Solution

    <AppSolution image='c-compiler-screenshot.png' href={appData.url}>
      This C compiler solution is designed to analyze user input line by line, store it in a 2D array, and identify syntax errors such as missing semicolons, incorrect parenthesis usage, and incorrect loop structures.
      
      The program processes each line of code and compares it against predefined rules. The logic identifies:
      - Missing semicolons (`;`)
      - Unmatched parentheses `()`
      - Incorrect use of `int main()`, `return`, `scanf`, and `printf`
      - Common errors in various loops and array declarations (1D arrays, etc.)
      - 15 distinct syntax errors in total
      
      The user is prompted to input code until "exit" is entered. Afterward, the program outputs a list of errors in the syntax.
    </AppSolution>
  </Section>

  <Border className="my-8" />

  <Section id="technologies">
    ## Technologies

    <FadeInStagger className="flex gap-4 mt-6 flex-wrap" faster once>
      {technologies.map((tech) => (
        <FadeIn key={tech.name}>
          <div className="mt-auto">
            <Image
              src={tech.image}
              className="object-contain rounded-md m-auto"
              alt=""
              height={64}
              width={64}
              style={{
                width: 64,
                height: 64,
              }}
            />
            <h4 className="text-sm font-semibold tracking-tight text-[#525df3] text-center bg-white rounded-full w-min px-2 m-2 mx-auto">{tech.name}</h4>
          </div>
        </FadeIn>
      ))}
    </FadeInStagger>
  </Section>

</Container>
