import { AppSolution, Border, Button, Container, FadeIn, FadeInStagger, Section } from '@/components';
import Image from 'next/image';
import { sierraEcomaderas } from '../../../../public/projects'

export const appData = {
  industry: 'Travle and Tourism',
  title: 'bus reservation application',
  description: 'The **Bus Reservation Application** is a system designed to streamline the booking, cancellation, and management of bus tickets, providing users with an efficient and user-friendly interface to reserve their travel.',
  image: sierraEcomaderas,
  date: '2021-10',
  service: 'Static Website',
  url: 'https://github.com/nikhil8424/Buss_Reservation_aaplication',
  pathname: '/apps/sierra-ecomaderas',
  framework: 'next',
};

export const sections = [
  { index: 0, title: 'About', id: 'about' },
  { index: 1, title: 'Challenge', id: 'challenge' },
  { index: 2, title: 'Solution', id: 'solution' },
  { index: 3, title: 'Technologies', id: 'technologies' },
];

export const metadata = {
  title: `${appData.title}`,
  description: appData.description,
};

export const technologies = [
  {
    name: 'Python',
    image: '/logos/python-logo.png',
  },
  
];

<Container>

<Border className="my-8" />

  <Section id="challenge">
    ## Challenge

1. User Interface Complexity: Designing a user-friendly interface for bus reservation, cancellation, and feedback management.
2. Data Integrity: Ensuring the accuracy and consistency of bus reservation data, especially with multiple users accessing the system simultaneously.
3. Real-Time Updates: Implementing real-time bus seat availability tracking and updates during reservation and cancellation processes.
4. Security and Authentication: Safeguarding user data and providing secure login, signup, and password recovery mechanisms.

  </Section>

<Border className="my-8" />

<Section id="solution">

## Solution

<AppSolution href={appData.url}>
Solution:
Intuitive UI Design: Develop a clean, easy-to-navigate interface using Python libraries like Tkinter for a seamless user experience.
Database Consistency: Use an SQLite or MySQL database with ACID properties to ensure data integrity and avoid conflicts during concurrent user access.
Dynamic Seat Availability: Implement algorithms to update seat availability in real-time and reflect changes immediately after reservations or cancellations.
Robust Security: Incorporate encryption techniques for password storage and use secure authentication methods to protect user accounts.
  
</AppSolution>

</Section>

<Border className="my-8" />

<Section id="technologies">

## Technologies

<FadeInStagger className="flex gap-4 mt-6 flex-wrap" faster once>
  {technologies.map((tech) => (
    <FadeIn key={tech.name}>
      <div className="mt-auto">
        <Image
          src={tech.image}
          className="object-contain rounded-md m-auto"
          alt=""
          height={64}
          width={64}
          style={{
            width: 64,
            height: 64,
          }}
        />
        <h4 className="text-sm font-semibold tracking-tight text-[#525df3] text-center bg-white rounded-full w-min px-2 m-2 mx-auto">{tech.name}</h4>
      </div>
    </FadeIn>
  ))}
</FadeInStagger>

</Section>

</Container>
